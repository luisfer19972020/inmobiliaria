server:
  port: 8003
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost
    fetch-registry: true
    register-with-eureka: true