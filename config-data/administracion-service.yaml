server:
  port: 8001
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost
    fetch-registry: true
    register-with-eureka: true

